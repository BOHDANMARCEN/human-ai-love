# 🔥 Firebase Setup Guide

Ця інструкція допоможе вам налаштувати Firebase для роботи чату та завантаження файлів на сайті.

## 📋 Крок 1: Створення Firebase проекту

1. Перейдіть на [Firebase Console](https://console.firebase.google.com/)
2. Натисніть **"Add project"** або **"Create a project"**
3. Введіть назву проекту (наприклад: `human-ai-love`)
4. Виберіть чи потрібен Google Analytics (опціонально)
5. Натисніть **"Create project"** та дочекайтесь завершення

## 🔐 Крок 2: Налаштування Authentication

1. У лівому меню виберіть **Authentication**
2. Натисніть **"Get started"**
3. Перейдіть на вкладку **"Sign-in method"**

### GitHub Provider:
1. Натисніть на **"GitHub"**
2. Увімкніть перемикач
3. Вам потрібно створити OAuth App на GitHub:
   - Перейдіть на [GitHub Developer Settings](https://github.com/settings/developers)
   - Натисніть **"New OAuth App"**
   - **Application name**: `Human × AI Chat`
   - **Homepage URL**: `https://your-username.github.io` (або ваш домен)
   - **Authorization callback URL**: `https://YOUR_PROJECT_ID.firebaseapp.com/__/auth/handler`
   - Скопіюйте **Client ID** та **Client Secret**
4. Вставте **Client ID** та **Client Secret** у Firebase
5. Натисніть **"Save"**

### Google Provider:
1. Натисніть на **"Google"**
2. Увімкніть перемикач
3. Виберіть підтримуваний email (або залиште за замовчуванням)
4. Натисніть **"Save"**

### Authorized Domains:
1. У розділі **"Authorized domains"** додайте:
   - `your-username.github.io` (якщо використовуєте GitHub Pages)
   - Ваш кастомний домен (якщо є)
   - `localhost` (для локального тестування)

## 💾 Крок 3: Налаштування Firestore Database

1. У лівому меню виберіть **Firestore Database**
2. Натисніть **"Create database"**
3. Виберіть **"Start in test mode"** (для початку)
4. Виберіть регіон (найближчий до вас)
5. Натисніть **"Enable"**

### Security Rules (рекомендовано):
Після тестування оновіть правила безпеки:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Messages collection
    match /messages/{messageId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
  }
}
```

## 📦 Крок 4: Налаштування Storage

1. У лівому меню виберіть **Storage**
2. Натисніть **"Get started"**
3. Виберіть **"Start in test mode"** (для початку)
4. Виберіть регіон (той самий, що для Firestore)
5. Натисніть **"Done"**

### Security Rules (рекомендовано):
Після тестування оновіть правила безпеки:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{userId}/{fileName} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

## 🔑 Крок 5: Отримання конфігурації

1. У лівому меню виберіть **Project Settings** (⚙️)
2. Прокрутіть до розділу **"Your apps"**
3. Натисніть на іконку **Web** (`</>`)
4. Введіть назву додатку (наприклад: `Human × AI Website`)
5. Скопіюйте конфігураційний об'єкт

## 📝 Крок 6: Додавання конфігурації до проекту

1. Відкрийте файл `assets/js/firebase-config.js`
2. Замініть значення в об'єкті `firebaseConfig`:

```javascript
const firebaseConfig = {
  apiKey: "YOUR_API_KEY",                    // ← Замініть
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",  // ← Замініть
  projectId: "YOUR_PROJECT_ID",             // ← Замініть
  storageBucket: "YOUR_PROJECT_ID.appspot.com",   // ← Замініть
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",  // ← Замініть
  appId: "YOUR_APP_ID"                       // ← Замініть
};
```

## ✅ Крок 7: Перевірка роботи

1. Відкрийте `chat.html` у браузері
2. Спробуйте увійти через GitHub або Google
3. Надішліть тестове повідомлення
4. Перевірте, чи з'являється воно в чаті
5. Відкрийте `upload.html` та спробуйте завантажити файл

## 🔒 Безпека (важливо!)

### Після тестування обов'язково:

1. **Firestore Rules** - обмежте доступ до повідомлень:
   ```javascript
   match /messages/{messageId} {
     allow read: if request.auth != null;
     allow create: if request.auth != null 
                   && request.resource.data.userId == request.auth.uid;
     allow delete: if request.auth != null 
                   && resource.data.userId == request.auth.uid;
   }
   ```

2. **Storage Rules** - обмежте розмір файлів:
   ```javascript
   match /uploads/{userId}/{fileName} {
     allow write: if request.auth != null 
                 && request.auth.uid == userId
                 && request.resource.size < 10 * 1024 * 1024; // 10MB
   }
   ```

3. **Authentication** - додайте обмеження на домени в налаштуваннях

## 🐛 Вирішення проблем

### Помилка "Firebase SDK not loaded"
- Перевірте, чи підключені Firebase скрипти в HTML перед `firebase-config.js`
- Перевірте CSP (Content Security Policy) у `<head>` сторінки

### Помилка авторизації
- Перевірте, чи додано ваш домен до Authorized domains
- Перевірте правильність Client ID та Client Secret для GitHub

### Повідомлення не з'являються
- Перевірте правила Firestore
- Перевірте консоль браузера на наявність помилок

### Файли не завантажуються
- Перевірте правила Storage
- Перевірте розмір файлу (максимум 10MB)
- Перевірте консоль браузера на наявність помилок

## 📚 Додаткові ресурси

- [Firebase Documentation](https://firebase.google.com/docs)
- [Firebase Authentication](https://firebase.google.com/docs/auth)
- [Firestore Documentation](https://firebase.google.com/docs/firestore)
- [Firebase Storage](https://firebase.google.com/docs/storage)

## 💡 Поради

- Використовуйте тестовий режим тільки для розробки
- Завжди налаштовуйте правила безпеки перед публікацією
- Регулярно перевіряйте логи у Firebase Console
- Налаштуйте обмеження на розмір файлів та кількість повідомлень

---

**Готово!** 🎉 Тепер ваш сайт має повноцінний чат з логуванням та можливість завантаження файлів.

